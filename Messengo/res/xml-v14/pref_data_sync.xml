<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <Preference 
        android:title="@string/pref_title_save_sms"
		android:summary="Sauvegarde tes sms sur le cloud, comme ça, tu les auras toujours avec toi!"
        android:key="saveSms"
        />

    <Preference android:title="@string/pref_title_clean_local"
        android:summary="Supprime tout les données de l'application sur le téléphone"
		android:key="clean"/>
    	    
</PreferenceScreen>